/**
 * Type declaration for plotly.js-dist-min
 * The minified bundle doesn't ship its own .d.ts so we point to the full types
 */
declare module 'plotly.js-dist-min' {
    export * from 'plotly.js';
}